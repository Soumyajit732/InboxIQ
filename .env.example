OPENAI_API_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
APP_ENV=development
# Optional overrides -- if unset, config.js falls back to hardcoded values based on APP_ENV
REDIRECT_URI=
FRONTEND_URL=

# Python NLP/vector service (this branch only -- not used on main)
SPACY_SERVICE_URL=http://localhost:8001
